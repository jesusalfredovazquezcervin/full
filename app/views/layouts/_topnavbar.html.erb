<div class="row border-bottom" xmlns="http://www.w3.org/1999/html">
    <nav class="navbar navbar-fixed-top" role="navigation" style="margin-bottom: 0">
        <div class="navbar-header">

          <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#"><i class="fa fa-bars"></i> </a>

        </div>
      <div class="navbar-header">
        <%= form_for @capture , url: {controller:"captures", action: "consultar"}, class: "navbar-form-custom" do |f| %> 
            <div>
            <%= f.select :cliente_id, options_for_select(@clientes.order(:nombreComercial).collect { |c|
                                                           [(c.nombreComercial.nil? == true ? (c.nombre):(c.nombreComercial)) << ' - ' << c.datosgenerale.frase, c.id ] }),
                         {prompt: 'Seleccione cuenta'}, { class:"chosen-select", style: "width:500px;", :required => true } %>    

              <%= button_tag(type: 'submit', class: "btn btn-primary") do %>
                  <i class="fa fa-search"></i>
              <% end %>
              </div>



        <%end%> 
      </div>



        <ul class="nav navbar-top-links navbar-right">
            <%if !@cliente.nil?%>
              <li class="dropdown">
                <a class="dropdown-toggle count-info" data-toggle="dropdown" href="#">
                  <i class="fa fa-bell"></i>
                  <% if @cliente.notifications.active(@cliente).count > 0%>
                    <span class="label label-warning"><%=@cliente.notifications.active(@cliente).count%></span>
                  <%end%>
                </a>
                <ul class="dropdown-menu dropdown-messages">
                  <% @cliente.notifications.active(@cliente).each do |notification| %>
                      <li>
                        <div class="dropdown-messages-box">
                          <a href="#" class="pull-left">
                            <img alt="image" class="img-circle" src="<%= image_path('profile.jpg') %>" />
                          </a>
                          <div class="media-body ">
                            <small class="pull-right">&nbsp;&nbsp;&nbsp;hace <%= distance_of_time_in_words(notification.startdatetime + DateTime.now.utc_offset*(-1).second, DateTime.now).to_s  %></small>
                            <strong><%=notification.person.nombre%></strong>&nbsp;<%=notification.event.event%>
                            <%=notification.notification.nil? == false ? (notification.notification):('') %>
                            <strong>
                              <%=notification.recipient.nil? == false ? (notification.recipient.nombre):('') %>
                            </strong><br/>
                            <small class="text-muted">Vence en <%= distance_of_time_in_words(DateTime.now, notification.duedatetime + DateTime.now.utc_offset*(-1).second).to_s  %>

                            </small>
                            <small class="pull-right">&nbsp;&nbsp;&nbsp;

                            </small>
                            <small class="pull-right">
                              <%= link_to edit_notification_path(notification), class:"text-muted" do%>
                                  <i class="fa fa-pencil"></i>
                              <% end %>
                              <%= link_to notification, method: :delete, class:"text-muted", data: { confirm: '¿Está seguro?'} do%>
                                  <i class="fa fa-trash"></i>
                              <% end %>
                            </small>


                          </div>
                        </div>
                      </li>
                      <li class="divider"></li>
                  <%end%>
                  <li>
                    <div class="text-center link-block">

                      <button type="button" class="btn btn-primary btn-xs" data-toggle="modal" data-target="#myModal">
                        Crear notificacion</button>


                    </div>
                  </li>
                </ul>
              </li>
            <%end%>




            <li>

                <%=link_to "Salir",  destroy_usuario_session_path, method: :delete%>



            </a>
        </li>

            <li>
                <a class="right-sidebar-toggle">
                    <i class="fa fa-tasks"></i>
                </a>
            </li>
    </ul>
</nav>
</div>

<div class="modal inmodal" id="myModal" tabindex="-1" role="dialog" aria-hidden="true">
  <%if !@cliente.nil?%>
    <%= render 'notifications/form' %>
  <%end%>
</div>
<% content_for :javascript do %>

    <script type="text/javascript">


        var config = {
            '.chosen-select'           : {},
            '.chosen-select-deselect'  : {allow_single_deselect:true},
            '.chosen-select-no-single' : {disable_search_threshold:10},
            '.chosen-select-no-results': {no_results_text:'Oops, nothing found!'},
            '.chosen-select-width'     : {width:"95%"}
        }
        for (var selector in config) {
            $(selector).chosen(config[selector]);
        }

        $('#myModal').on('shown.bs.modal', function (e) {
        // Initialise external plugins for controls
            //require chosen/chosen.jquery.js


        })
    </script>
<% end %>
